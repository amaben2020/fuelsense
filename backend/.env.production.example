PORT=5001
TCP_PORT=5027
NODE_ENV=production
JWT_SECRET=use-a-long-random-secret-in-production
JWT_EXPIRES_IN=7d

# Neon PostgreSQL (production) — replace with your credentials
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
